# TODO

## Files that needs work

- [ ] psputility_*.h
  - [ ] Turn defines to enum (and rename to use Sce instead of PSP)
  - [ ] Rename fields when possible

- [ ] pspnet_*.h / psphttp.h / pspssl
  - [ ] Turn defines to enum (and rename to use Sce instead of PSP)
  - [ ] Rename fields when possible
  - [ ] Add linking notes

- [ ] pspthreadman.h
  - [ ] Turn defines to enum (and rename to use Sce instead of PSP)
  - [ ] Rename fields when possible